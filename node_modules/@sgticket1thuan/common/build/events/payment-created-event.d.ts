import { Subject } from "./subject";
export interface PaymentCreatedEvent {
    subject: Subject.PaymentCreated;
    data: {
        id: string;
        stripeId: string;
        orderId: string;
    };
}
